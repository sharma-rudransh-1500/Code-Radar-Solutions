#include <stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    if (n>o) {
        printf("Positive");
    }
    else if (n<0) {
        printf("Negative");
    }
    else if (n==0) {
        printf("Zero");
    }
    else {
        printf("Enter Valid Choice");
    }
    return 0;
}